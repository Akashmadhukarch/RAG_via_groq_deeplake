# RAG_via_groq_deeplake
🔹 Embeddings in RAG 

In Retrieval-Augmented Generation:

Split document into chunks

Convert chunks → embeddings

Store in vector database

Convert user query → embedding

Find nearest vectors

Send retrieved text to LLM

Embeddings are the bridge between:
📄 Documents
and
🤖 LLM reasoning

Without embeddings → No semantic retrieval.
